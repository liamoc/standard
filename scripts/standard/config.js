var Config = Class.extend({
	constructor: null,

	controls: {
		up: KEY_UP,
		down: KEY_DOWN,
		left: KEY_LEFT,
		right: KEY_RIGHT,
		accept: KEY_X,
		cancel: KEY_Z
	},
	
	volume: {
		music: 0,
		sound: 10
	}
});